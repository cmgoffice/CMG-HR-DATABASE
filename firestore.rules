rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาต read/write ทุก path (ใช้กับ project cmg-hr-database)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
